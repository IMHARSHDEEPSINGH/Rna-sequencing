# scRNA-seq R Environment Setup

This repository installs all required packages for transcriptomics / single-cell analysis.

## Setup

Open R or RStudio and run:

source("setup_scRNA_env.R")

## Verify installation

source("test_environment.R")

If no errors appear, the environment is ready.
